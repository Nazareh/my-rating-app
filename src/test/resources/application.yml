spring:
  cloud:
    gcp:
      project-id: my-rating-app-auth-test
      credentials:
        location:
      firestore:
        emulator:
          enabled: true
#  data:
#    mongodb:
#      database: my-rating-app
#      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017}
  security:
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.4.0
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
